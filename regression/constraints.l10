// Constraints

w: world.

a: t -> t -> rel @ w.
b: t -> rel @ w.
c: rel @ w.

a X X -> b X.

db1 = (a foo bar, a foo baz, a bar baz, a bar bar) @ w.
db2 = (a foo bar, a foo baz, a bar baz, a bar foo) @ w.